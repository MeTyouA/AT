<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4"
      main_tree_to_execute="RMUC2026_attack">
  <BehaviorTree ID="RMUC2026_attack">
    <ReactiveSequence>
      <SubRobotStatus topic_name="/robot_status"
                      robot_status="{robot_status}"/>
      <SubGameStatus topic_name="/game_status"
                     game_status="{game_status}"/>
      <Fallback>
        <IsGameTime message="{game_status}"
                    game_progress="4"
                    lower_remain_time="0"
                    higher_remain_time="100000"/>
        <IsAttacked message="{robot_status}"/>
      </Fallback>
      <ReactiveFallback name="ModeSelector">
        <ReactiveSequence name="PatrolSequence">
          <IsGameTime message="{game_status}"
                      game_progress="4"
                      lower_remain_time="240"
                      higher_remain_time="1000"/>
          <Sequence>
            <RobotControl stop_gimbal_scan="True"
                          chassis_spin_vel="10.0"
                          gimbal_big_yaw_vel="0.0"
                          gimbal_big_yaw_angle="0.0"
                          follow_gimbal_big="False"
                          chassis_vel_multi="1.0"
                          stop_chassis_vel_multi="True"
                          track_status="False"
                          perception_status="True"
                          start_gimbal_big_spin="False"/>
            <LoadWaypoints waypoint_file="/home/sentry/Desktop/ros_ws/csv/中央高地前3分钟保卫点.csv"
                           frame_id="map"
                           waypoints="{waypoints_high}"
                           wait_times="{wait_times_high}"
                           total_waypoints="{total_wp_high}"
                           wp_idx="{wp_idx_high}"/>
            <KeepRunningUntilFailure>
              <Sequence>
                <GetCurrentWaypoint waypoints="{waypoints_high}"
                                    wait_times="{wait_times_high}"
                                    wp_idx="{wp_idx_high}"
                                    current_goal="{current_goal_high}"
                                    current_wait_sec="{current_wait_sec_high}"/>
                <ReactiveSequence>
                  <PubNav2Goal goal_pose="{current_goal_high}"
                               frame_id="map"
                               min_pub_interval_ms="500"/>
                  <WaitUntilReached goal_pose="{current_goal_high}"
                                    tolerance="0.5"/>
                </ReactiveSequence>
                <WaitDuration duration_sec="{current_wait_sec_high}"/>
                <NextWaypoint wp_idx="{wp_idx_high}"
                              total_waypoints="{total_wp_high}"/>
              </Sequence>
            </KeepRunningUntilFailure>
          </Sequence>
        </ReactiveSequence>
        <ReactiveSequence name="PatrolSequence">
          <IsGameTime message="{game_status}"
                      game_progress="4"
                      lower_remain_time="0"
                      higher_remain_time="240"/>
          <Sequence name="PatrolNavigation">
            <RobotControl stop_gimbal_scan="True"
                          chassis_spin_vel="10.0"
                          gimbal_big_yaw_vel="0.0"
                          gimbal_big_yaw_angle="0.0"
                          follow_gimbal_big="False"
                          chassis_vel_multi="1.0"
                          stop_chassis_vel_multi="True"
                          track_status="False"
                          perception_status="True"
                          start_gimbal_big_spin="False"/>
            <LoadWaypoints waypoint_file="/home/sentry/Desktop/ros_ws/csv/中央高地后4分钟保卫点.csv"
                           frame_id="map"
                           waypoints="{waypoints_patrol}"
                           wait_times="{wait_times_patrol}"
                           total_waypoints="{total_wp_patrol}"
                           wp_idx="{wp_idx_patrol}"/>
            <KeepRunningUntilFailure name="PatrolWaypointLoop">
              <Sequence name="NavigatePatrolWaypoint">
                <GetCurrentWaypoint waypoints="{waypoints_patrol}"
                                    wait_times="{wait_times_patrol}"
                                    wp_idx="{wp_idx_patrol}"
                                    current_goal="{current_goal_patrol}"
                                    current_wait_sec="{current_wait_patrol}"/>
                <ReactiveSequence name="PatrolSequence">
                  <PubNav2Goal goal_pose="{current_goal_patrol}"
                               frame_id="map"
                               min_pub_interval_ms="500"/>
                  <WaitUntilReached goal_pose="{current_goal_patrol}"
                                    tolerance="1.0"/>
                </ReactiveSequence>
                <WaitDuration duration_sec="{current_wait_patrol}"/>
                <NextWaypoint wp_idx="{wp_idx_patrol}"
                              total_waypoints="{total_wp_patrol}"/>
              </Sequence>
            </KeepRunningUntilFailure>
          </Sequence>
        </ReactiveSequence>
      </ReactiveFallback>
    </ReactiveSequence>
  </BehaviorTree>

  <!-- Description of Node Models (used by Groot) -->
  <TreeNodesModel>
    <Action ID="GetCurrentWaypoint">
      <input_port name="waypoints"
                  default="{waypoints}"/>
      <input_port name="wait_times"
                  default="{wait_times}"/>
      <input_port name="wp_idx"
                  default="{wp_idx}"/>
      <output_port name="current_goal"
                   default="{current_goal}"/>
      <output_port name="current_wait_sec"
                   default="{current_wait}"/>
    </Action>
    <Action ID="IsAttacked"
            editable="true">
      <input_port name="message"
                  default="{robot_status}"/>
    </Action>
    <Condition ID="IsGameTime"
               editable="true">
      <input_port name="message"
                  default="{game_status}"/>
      <input_port name="game_progress"
                  default="4"/>
      <input_port name="lower_remain_time"
                  default="0"/>
      <input_port name="higher_remain_time"
                  default="300"/>
    </Condition>
    <Action ID="LoadWaypoints">
      <input_port name="waypoint_file"/>
      <input_port name="frame_id"
                  default="map"/>
      <output_port name="waypoints"
                   default="{waypoints}"/>
      <output_port name="wait_times"
                   default="{wait_times}"/>
      <output_port name="total_waypoints"
                   default="{total_wp}"/>
      <output_port name="wp_idx"
                   default="{wp_idx}"/>
    </Action>
    <Action ID="NextWaypoint">
      <inout_port name="wp_idx"
                  default="{wp_idx}"/>
      <input_port name="total_waypoints"
                  default="{total_wp}"/>
    </Action>
    <Action ID="PubNav2Goal">
      <input_port name="goal_pose"/>
      <input_port name="frame_id"
                  default="map"/>
      <input_port name="min_pub_interval_ms"/>
    </Action>
    <Action ID="RobotControl">
      <input_port name="stop_gimbal_scan"
                  default="True"/>
      <input_port name="chassis_spin_vel"
                  default="0.0"/>
      <input_port name="gimbal_big_yaw_vel"
                  default="1.0"/>
      <input_port name="gimbal_big_yaw_angle"
                  default="0.785"/>
      <input_port name="follow_gimbal_big"
                  default="true"/>
      <input_port name="chassis_vel_multi"
                  default="1.0"/>
      <input_port name="stop_chassis_vel_multi"
                  default="True"/>
      <input_port name="track_status"
                  default="false"/>
      <input_port name="perception_status"
                  default="True"/>
      <input_port name="start_gimbal_big_spin"
                  default="False"/>
    </Action>
    <Action ID="SubGameStatus"
            editable="true">
      <input_port name="topic_name"
                  default="game_status"/>
      <output_port name="game_status"
                   default="{game_status}"/>
    </Action>
    <Action ID="SubRobotStatus"
            editable="true">
      <input_port name="topic_name"
                  default="robot_status"/>
      <output_port name="robot_status"
                   default="{robot_status}"/>
    </Action>
    <Action ID="WaitDuration">
      <input_port name="duration_sec"
                  default="5.0"/>
    </Action>
    <Action ID="WaitUntilReached">
      <input_port name="goal_pose"/>
      <input_port name="tolerance"
                  default="0.5"/>
    </Action>
  </TreeNodesModel>

</root>
